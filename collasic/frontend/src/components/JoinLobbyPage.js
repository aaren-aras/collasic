import React, { Component } from "react";
// Don't need 'import { render } from "react-dom"' since its being rendered FROM 'app' component

export default class JoinLobbyPage extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return <p>Let's Join Some Lobbies!</p>
    }
} 